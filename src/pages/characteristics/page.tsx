import { CharacteristicsList } from '@/features/characteristics';
import { View } from 'tamagui';

export const Characteristics = () => {
  return (
    <View flex={1}>
      <CharacteristicsList />
    </View>
  );
};
